# Assume names references a list. Write a for loop that displays each element of the list.

names = ['Einstein', 'Newton', 'Copernicus', 'Kepler']

# Using a for loop to display each element in the list
for name in names:
    print(name)